# Structure React (Vite)

## Scripts
- `npm install` installe les dépendances
- `npm run dev` lance le serveur de dev
- `npm run build` build de production
- `npm run preview` prévisualise le build

## Structure
- `src/components` composants UI
- `src/pages` pages du site
- `src/routes` définitions des routes
- `src/assets` images, fonts, etc.
- `public` fichiers statiques (ex: favicon)

## Notes
- Router déjà configuré (React Router v6)
- Point d'entrée: `src/main.jsx`, `src/App.jsx`

Dis-moi la prochaine étape et je l’implémente.
